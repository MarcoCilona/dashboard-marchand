import React from 'react';
import { render } from '@testing-library/react';

import RecapCardList from './RecapCardList';

describe('RecapCardList tests', () => {
  test('Renders component RecapCardList', () => {
    render(<RecapCardList />);
  });
});
